{
  "name": "hydra",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package meteortesting:mocha"
  },
  "dependencies": {
    "@babel/runtime": "^7.3.4",
    "@types/meteor": "^1.4.28",
    "meteor-node-stubs": "^0.4.1"
  },
  "devDependencies": {
    "chai": "^4.1.2"
  }
}
